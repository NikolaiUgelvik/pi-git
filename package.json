{
  "name": "pi-git-tui",
  "version": "0.1.2",
  "description": "Interactive Git diff, staging, history, and commit TUI for Pi.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "git",
    "diff"
  ],
  "license": "Apache-2.0",
  "author": {
    "name": "Nikolai Ugelvik",
    "email": "nikolaiu@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NikolaiUgelvik/pi-git-tui.git"
  },
  "homepage": "https://github.com/NikolaiUgelvik/pi-git-tui#readme",
  "bugs": {
    "url": "https://github.com/NikolaiUgelvik/pi-git-tui/issues"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "files": [
    "extensions",
    "src",
    "assets",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./extensions/diff.ts"
    ],
    "image": "https://raw.githubusercontent.com/NikolaiUgelvik/pi-git-tui/main/assets/banner.png"
  },
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "dev": "pi -e ./extensions/diff.ts",
    "format": "biome check --write .",
    "lint": "biome check .",
    "lint:ci": "biome ci .",
    "check": "node ./scripts/check.mjs",
    "test": "node ./scripts/test.mjs --all",
    "test:coverage": "node ./scripts/clean.mjs --coverage && c8 --reporter=json --reports-dir=.tmp-coverage node ./scripts/test.mjs --all && node ./scripts/sanitize-coverage.mjs",
    "test:file": "node ./scripts/test.mjs --file",
    "test:watch": "node ./scripts/test.mjs --watch",
    "test:clean": "node ./scripts/clean.mjs --tests",
    "smoke:package": "node ./tests/package-smoke.mjs",
    "benchmark:git": "node benchmarks/git-pipeline.mjs",
    "benchmark:render": "node benchmarks/render-caches.mjs",
    "benchmark:load": "node benchmarks/extension-loading.mjs",
    "benchmark:loop": "node benchmarks/developer-loop.mjs",
    "benchmark:ci": "npm run benchmark:git -- --iterations 1 && npm run benchmark:render && npm run benchmark:load -- --iterations 20 && npm run benchmark:loop -- --assert",
    "fallow": "npm run fallow:dead-code && npm run fallow:audit",
    "fallow:dead-code": "fallow dead-code --ci --fail-on-issues",
    "fallow:audit": "fallow audit --gate new-only --coverage .tmp-coverage/coverage-final.json --ci",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "typecheck:watch": "tsc -p tsconfig.typecheck.json --watch",
    "typecheck:clean": "node ./scripts/clean.mjs --typecheck",
    "prepare": "node ./scripts/prepare.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@earendil-works/pi-tui": "^0.83.0",
    "@types/node": "^26.1.2",
    "c8": "^12.0.0",
    "fallow": "^3.10.0",
    "husky": "^9.1.7",
    "typescript": "^7.0.2"
  }
}
